<h1>Editing event_line</h1>

<%= error_messages_for :event_line %>

<% form_for(:event_line, :url => event_line_path(@event_line), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', event_line_path(@event_line) %> |
<%= link_to 'Back', event_lines_path %>